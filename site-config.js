/**
 * URL canonique du site — référence unique pour les scripts (Open Graph, liens absolus, etc.).
 * À utiliser avec : const origin = window.SITE_ORIGIN;
 */
window.SITE_ORIGIN = 'https://comptal2.org';
